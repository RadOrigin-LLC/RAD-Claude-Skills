{
  "name": "rad-code-review",
  "version": "4.1.0",
  "description": "3-role adversarial code review for Claude Code, optimized for Opus 4.7 (Sonnet 4.6 / Haiku 4.5 compatible). Parallel tool-call pipeline, JSON-first subagent output, compaction-safe checkpointing with `--resume`, and `--non-interactive` mode for agents/CI. Blame-aware diff scoping, 14-pattern AI slop detection (hallucinated imports, fake error handling, ghost type assertions, mock-shaped fallbacks, etc.), framework-specific IDOR for Next.js / Express / Fastify / Django / Rails / Go, WCAG 2.2 + dynamic ARIA state detection, performance heuristics, severity-ranked findings with release verdict, accepted-risk expiry enforcement. Includes a `code-reviewer` agent for proactive autonomous review.\n\n**v4.1 additions.** `scripts/check-hallucinated-imports.py` — mechanical detection of AI slop Pattern #1 (Hallucinated Imports). Pure-stdlib Python 3.8+ that parses 9 lockfile formats (package-lock.json, yarn.lock, pnpm-lock.yaml, package.json deps, requirements.txt, pyproject.toml, Pipfile.lock, poetry.lock, uv.lock), extracts imports via Python `ast` + JS/TS regex, classifies stdlib / builtin / relative / external / scoped / submodule, and flags packages not declared in any lockfile (slopsquatting risk). Emits RADCR-HALLUC-NNN findings consumable by the review report. Standalone or invoked from the orchestrator's automated-checks phase.\n\n**v4.0 breaking change.** The `UCR-` heritage prefix on config (`.ucrconfig.yml`), finding IDs (`UCR-NNN`), and state/history directories (`.ucr/`) was renamed to `RADCR-` / `.radcrconfig.yml` / `.radcr/` for naming consistency with the plugin. Existing users on v3.x must rename `.ucrconfig.yml` → `.radcrconfig.yml` and `.ucr/` → `.radcr/` (no content changes required). See README's Naming section for details.",
  "author": {
    "name": "RAD",
    "url": "https://github.com/radesjardins"
  },
  "license": "Apache-2.0",
  "homepage": "https://github.com/radesjardins/RAD-Claude-Skills/tree/main/plugins/rad-code-review",
  "repository": "https://github.com/radesjardins/RAD-Claude-Skills",
  "keywords": ["code-review", "security", "owasp", "ai-slop", "diff-aware", "architecture", "performance", "accessibility", "release-readiness"]
}

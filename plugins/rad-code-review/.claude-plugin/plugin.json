{
  "name": "rad-code-review",
  "version": "5.1.0",
  "description": "3-role adversarial code review for Claude Code — Opus default, Sonnet/Haiku compatible. Parallel tool-call pipeline, JSON-first subagent output, compaction-safe checkpointing with `--resume`, and `--non-interactive` mode for agents/CI. Blame-aware diff scoping, 14-pattern AI slop detection (hallucinated imports, fake error handling, ghost type assertions, mock-shaped fallbacks, etc.), framework-specific IDOR for Next.js / Express / Fastify / Django / Rails / Go, WCAG 2.2 + dynamic ARIA state detection, performance heuristics, severity-ranked findings with release verdict, and accepted-risk expiry enforcement. Includes a `code-reviewer` agent for proactive autonomous review.\n\nv5.1: Backend-as-a-Service data-exposure lane — for apps where the database (RLS / Security Rules) is the authorization layer rather than server middleware. Covers Supabase/PostgREST RLS, Firebase rules, and Amplify/Appwrite/PocketBase/Nhost, with a reachability gate (exposed schema + grant + no policy) so deny-all and un-granted tables don't false-positive, plus the `user_metadata` untrusted-claim escalation and `service_role`/Admin keys leaked into client/public-env. New `--security-deep` mode (and `--strictness launch`) runs a 4-phase launch-readiness pass — trust boundaries → data-exposure surface → authorization model → secrets — under a no-false-assurance contract: it never emits a \"safe to launch\" verdict, reports verified-vs-could-not-verify, and recommends an independent human pen-test. Grounded in current advisories (CVE-2025-48757 missing-RLS class, Supabase publishable/secret key model, OWASP API Top 10).\n\nv5.0: finding IDs shortened to `CR-NNN` (config/state paths unchanged); the hallucinated-imports validator is now wired into the automated-checks phase (offline, lockfile-verified, runs even in --local-only); history comparison matches findings across runs by fingerprint (category+file+title) instead of per-run IDs, making \"show new findings only\" trustworthy; the never-implemented `--engine claude|codex|both` flag removed in favor of the real `--adversarial-model <name>` cross-model challenge pass; reports save to `.radcr/history/` only (no loose root-level report file).\n\nBacked by `check-hallucinated-imports.py` — a pure-stdlib Python 3.8+ script that parses 9 lockfile formats (package-lock.json, yarn.lock, pnpm-lock.yaml, package.json deps, requirements.txt, pyproject.toml, Pipfile.lock, poetry.lock, uv.lock), extracts imports via Python `ast` + JS/TS regex, and flags packages not declared in any lockfile (slopsquatting risk). Runs standalone or in the orchestrator's Step 5g.",
  "author": {
    "name": "RAD",
    "url": "https://github.com/RadOrigin-LLC"
  },
  "license": "Apache-2.0",
  "homepage": "https://github.com/RadOrigin-LLC/RAD-Claude-Skills/tree/main/plugins/rad-code-review",
  "repository": "https://github.com/RadOrigin-LLC/RAD-Claude-Skills",
  "keywords": ["code-review", "security", "owasp", "ai-slop", "diff-aware", "architecture", "performance", "accessibility", "release-readiness", "rls", "supabase", "baas"]
}
